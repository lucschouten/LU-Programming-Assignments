Linux

Compiler installeren:

In cmd  -> sudo apt-get update (updaten van installer)
        -> sudo apt-get install g++
        

Compilen van aitetris.cc bestand:

In cmd  -> cd (directory van het bestand)
        -> ls -ltr aitetris* (Controleren of je in de juiste directory zit)
        -> g++ -Wall -Wextra -O2 -o naamexecutable aitetris.cc
        
Executable runnen:
In cmd  -> ./naamexecutable (geeft informatie over de parameters)
Voorbeeld -> ./naamexecutable 20 15 1 1 2000 111
